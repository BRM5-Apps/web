"use client";

// JSON import/export for templates
export function JsonImport() {
  return (
    <div className="space-y-4 rounded-md border p-6">
      <p className="text-muted-foreground">JSON import/export — paste or upload template JSON</p>
    </div>
  );
}
