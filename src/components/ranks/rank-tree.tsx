"use client";

// Placeholder — renders a tree of ranks ordered by level
export function RankTree() {
  return (
    <div className="rounded-md border p-6 text-center text-muted-foreground">
      Rank hierarchy tree — wire up with rank data
    </div>
  );
}
