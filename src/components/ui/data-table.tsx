"use client";

// Placeholder — install @tanstack/react-table and build out columns/filtering
// when ready to implement real data tables.

export function DataTable() {
  return (
    <div className="rounded-md border">
      <p className="p-8 text-center text-muted-foreground">
        DataTable — wire up with @tanstack/react-table
      </p>
    </div>
  );
}
